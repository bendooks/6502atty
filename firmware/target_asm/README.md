# Build files for useful target code snippers

This contains the source and the built data as includable header-files for the firmware (and maybe as downloadable code at a later date). The built header files are checked in to avoid users having to rebuild them, as there is a requirement on the [64tass](https://github.com/irmen/64tass) assembler.

